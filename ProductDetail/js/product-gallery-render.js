document.addEventListener('DOMContentLoaded', () =>{
    const mainImageContainer = document.querySelector('.js-main-image');
    const subImagesContainer = document.querySelector('.js-sub-images-gallery');

    const mainImage = document.createElement('img');
});